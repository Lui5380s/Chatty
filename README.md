# 💬 ChatBot - [Ihr Projektname]



Ein kurzer, überzeugender Satz, der beschreibt, was Ihr ChatBot tut.

## ✨ Funktionen

* Feature 1: Kurze Erklärung (z.B. Kontextbewusste Antworten).
* Feature 2: Kurze Erklärung (z.B. Integration von externen APIs).
* Feature 3: Kurze Erklärung (z.B. Einfache Erweiterbarkeit der Dialoge).

## 🛠️ Tech Stack

* **Python**
* **Pixi** (Environment Management)
* **pytest** (Testing Framework)
* *Fügen Sie hier weitere Bibliotheken hinzu (z.B. TensorFlow, scikit-learn, OpenAI API, etc.)*

## 🚀 Installation & Setup

Dieses Projekt verwendet **Pixi** für das Environment Management, um eine konsistente Entwicklungsumgebung zu gewährleisten.

### Voraussetzungen

* [Pixi](https://pixi.sh) installiert auf Ihrem System.

### Schritte

1.  **Repository klonen:**
    ```bash
    git clone [https://github.com/Lui5380s/ChatBot.git](https://github.com/Lui5380s/ChatBot.git)
    cd ChatBot
    ```

2.  **Pixi-Umgebung einrichten:**
    Pixi liest die `pyproject.toml` und erstellt die Umgebung.
    ```bash
    pixi install
    ```

3.  **Umgebung aktivieren:**
    ```bash
    pixi shell
    ```

4.  **Starten des Bots:**
    ```bash
    pixi run start
    # Oder direkt: python main.py
    ```

## ✅ Tests ausführen

Stellen Sie sicher, dass Sie sich in der Pixi-Shell befinden (`pixi shell`):

```bash
pixi run test